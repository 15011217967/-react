const defaultState = {
    navList:[{
        navName:'首页',
        path:'/'
    },{
        navName:'订单页',
        path:'/order'
    }]
};

export default (state = defaultState, action) => {

    return state;
}